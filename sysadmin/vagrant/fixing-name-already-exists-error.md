Vagrant: fixing 'name already exists' error
===========================================

Make sure the appropriate ID listed here:

```
vboxmanage list vms
```

Matches the contents of:

```
.vagrant/machines/default/virtualbox/id
```
